import React from "react";
import { withRouter, RouteComponentProps } from "react-router-dom";

const StudentInfo = (props: RouteComponentProps) => {
	return <div>Hello Wolrd</div>;
};

export default withRouter(StudentInfo);
